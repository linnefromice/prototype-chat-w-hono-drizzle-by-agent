# Database Configuration (Legacy - not used with D1)
DATABASE_URL=postgres://postgres:password@localhost:5432/app_db

# Server Configuration (for Node.js development server only)
PORT=3000

# Better Auth Configuration
# Generate a random secret: openssl rand -hex 32
BETTER_AUTH_SECRET=your-secret-key-here

# Base URL Configuration
# Local development (HTTP): http://localhost:8787
# Production (HTTPS): https://your-app.workers.dev
BETTER_AUTH_URL=http://localhost:8787
BASE_URL=http://localhost:8787

# Notes:
# - For local development with Wrangler Dev, use HTTP (http://localhost:8787)
# - For production (Cloudflare Workers), BASE_URL is auto-detected as HTTPS
# - BETTER_AUTH_SECRET should be set as a Cloudflare Secret for production:
#   npx wrangler secret put BETTER_AUTH_SECRET
